# Services Guide

这个目录放跨多个 tool 共享的业务流程。

## 当前职责

- 浏览器搜索总编排
- 结果过滤与规范化
- 缓存命中与降级处理

## 这里的代码应该做到

- 不直接依赖 MCP 装饰器
- 不直接读环境变量
- 把可复用流程拆成稳定步骤

## 适用场景

当不同 provider、不同 tool 或不同调用路径共用同一条业务链路时，优先把逻辑放到这里。

