# Schemas Guide

本目录用于集中管理跨层流转的强类型共享数据结构与数据传输模型（Schemas）。

## 核心定位与原则
- **类型安全保障**: 统一使用 Pydantic 定义各层级（Service、Adapter、Tool）之间流动的高级数据对象（DTO），避免在接口间传递非结构化的裸 `dict`。
- **无状态模型**: 结构体仅包含字段定义与基本的参数验证规则，严禁注入具体业务运算逻辑、工具方法或全局配置项解析。
- **当前定义重点**: 统一的网页搜索结果模型、搜索交互响应、浏览器会话状态以及页面正文提取结果等。
