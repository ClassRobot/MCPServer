# Source Layout

`src/` 目录用于将项目的核心 Python 源代码与仓库中的配置文件、测试脚本及文档进行物理隔离。

## 物理隔离优势
- **导入行为一致**: 确保本地运行路径与正式发布（或 editable install）后的导入行为完全一致，避免依赖污染。
- **配置与测试分离**: 防止本地开发临时脚本、测试用例或缓存干扰可发布的 Python 包构建。

## 主包入口
- **[`mcp_server/`](mcp_server/README.md)**: 项目的核心 Python 包，承载服务装配、多层业务逻辑及接口定义。

